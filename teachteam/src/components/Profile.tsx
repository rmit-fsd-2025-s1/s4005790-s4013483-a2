export interface ProfileTutor {
  roles: string,
  availability: string;
  skills: string,
  credentials: string[]
};

export interface ProfileLecturer {
  age: string,
  contact: string,
  biography: string,
  links: string
};
