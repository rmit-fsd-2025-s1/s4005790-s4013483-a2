export interface ProfileTutor {
  roles: string,
  availability: string;
  skills: string,
  credentials: string[]
};

export interface ProfileLecturer {
  age: number,
  contact: string,
  biography: string,
  links: string
};
